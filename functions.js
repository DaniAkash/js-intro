const myFunction = function() {

};

let functionVariable = function() {

};

const arrowFunctions = () => {

};

const legacySum = function (a,b) {
    return a+b;
};

const sum = (a, b) => a+b; // function

sum(1,2); // function call

const square = a => a^2;

square(5);

a => a^2; // no use directly.